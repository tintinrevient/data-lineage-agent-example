{
  "mcpServers": {
    "oracle": {
      "command": "docker",
      "args": ["exec", "-i", "oracle-mcp", "python", "main.py"]
    },
    "dbt": {
      "command": "docker",
      "args": ["exec", "-i", "dbt-mcp", "dbt-mcp"]
    }
  }
}